
version: "1.1"
services:
  imageserver:
    container_name: ImageServer
    volumes:
      - /Users/nshcherbakova/Documents/dev/ImageEditor/ImageServer/data:/data
    build: .
    ports:
      - 81:8081
    restart: unless-stopped

